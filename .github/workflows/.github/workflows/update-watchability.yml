name: Update College Football Watchability Data

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Confirm workflow works
        run: echo "GitHub Actions is working"
